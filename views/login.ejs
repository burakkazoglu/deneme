<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Influencer Planner | Giriş</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="login-page">
    <div class="login-card">
      <img src="<%= settings.logoUrl %>" alt="Influencer Planner" class="login-logo" />
      <h1>Influencer Planner</h1>
      <p class="login-subtitle">Admin paneline hoş geldiniz.</p>
      <% if (error) { %>
        <div class="alert"><%= error %></div>
      <% } %>
      <form action="/login" method="POST">
        <label>
          E-posta
          <input type="email" name="email" required />
        </label>
        <label>
          Şifre
          <input type="password" name="password" required />
        </label>
        <button type="submit">Giriş Yap</button>
      </form>
      <div class="login-hint">
        <p>Demo admin: admin@planner.local / admin123</p>
        <p>Demo influencer: influencer@planner.local / infl123</p>
      </div>
    </div>
  </body>
</html>
